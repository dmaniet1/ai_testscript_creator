"""
Generated by CAPL-GEN
Project  : {{ project }}
Generated: {{ timestamp }}

conftest.py - pytest fixtures for CAN bus testing
Adapt the CANBusInterface class to your actual CAN hardware/library.
Common libraries: python-can, cantools, Vector XL Driver
"""
import pytest


class CANBusInterface:
    """
    Adapt this class to your actual CAN hardware.
    Examples:
      - python-can:  import can; bus = can.Bus(channel='PCAN_USBBUS1', bustype='pcan')
      - Vector XL:   use the vxlapi Python bindings
      - Simulation:  mock responses for unit testing
    """

    def __init__(self):
        # TODO: initialize your CAN hardware here
        # self.bus = can.Bus(channel='PCAN_USBBUS1', bustype='pcan')
        self._connected = False

    def connect(self):
        # TODO: connect to CAN bus
        self._connected = True

    def disconnect(self):
        # TODO: disconnect
        self._connected = False

    def wait_for_message(self, message_id: int, timeout: float):
        """Wait for a CAN message with given ID. Returns message or None."""
        # TODO: implement with your library
        # Example with python-can:
        # deadline = time.monotonic() + timeout
        # while time.monotonic() < deadline:
        #     msg = self.bus.recv(timeout=0.01)
        #     if msg and msg.arbitration_id == message_id:
        #         return msg
        raise NotImplementedError("Implement wait_for_message for your CAN hardware")

    def send_message(self, message_id: int, data: list):
        """Send a CAN message."""
        # TODO: implement with your library
        raise NotImplementedError("Implement send_message for your CAN hardware")

    def get_signal(self, signal_name: str):
        """Get current value of a named signal."""
        # TODO: implement signal access
        # This requires a .dbc database loaded via cantools
        raise NotImplementedError("Implement get_signal for your CAN hardware")

    def sleep(self, ms: int):
        import time
        time.sleep(ms / 1000.0)


@pytest.fixture(scope="session")
def can_bus():
    """Session-scoped CAN bus fixture — connects once, shared across all tests."""
    bus = CANBusInterface()
    bus.connect()
    yield bus
    bus.disconnect()
